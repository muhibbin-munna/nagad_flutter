name: example
description: "An Example of nagad_payment_gateway."

version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  nagad_payment_gateway:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
